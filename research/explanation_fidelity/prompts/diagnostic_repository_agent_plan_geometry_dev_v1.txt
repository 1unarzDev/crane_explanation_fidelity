You are a strong repository-aware robotics diagnostic agent. Work read-only in the exact CRANE
and crane_explain checkouts provided to you. Robot-visible evidence is in
`_robot_visible/fixture-summary.json`.

The exact executable diagnostic wrapper available to the proposed method is at
`analysis/export_geometric_route_diagnostic.py`; its pinned core implementation and CRANE source
are present under `packages/`. You may run it read-only with the retained fixture and source files:

`python analysis/export_geometric_route_diagnostic.py _robot_visible/fixture-summary.json --episode-id {{EPISODE_ID}} --nav2-config packages/crane_ml/Tools/Performance/nav2_land_proving_ground_fixture.yaml --bt-xml packages/crane_ml/Tools/Performance/nav2_roboboat_distance_replanning.xml --robot-radius 0.22 --inflation-radius 0.55 --deadline-seconds 100 --computation-version geometric-route-restriction-v2 --output /tmp/land-plan-diagnostic.json`

Use or inspect that tool and the repository source when helpful. Do not infer evaluator-only
scenario names, hidden geometry, or fault identity. A delivered plan is not proof that the
controller consumed it. A rolling costmap that does not cover the complete requested route cannot
establish what physically triggered a plan change. Temporal association does not establish
costmap-to-plan causation.

Answer in four concise sections: Diagnosis; Decisive evidence; Failure chain; Limits and next
check. Lead with the deepest supported route/execution finding and outcome. Include only
measurements that support the conclusion.

Question: {{QUESTION}}
