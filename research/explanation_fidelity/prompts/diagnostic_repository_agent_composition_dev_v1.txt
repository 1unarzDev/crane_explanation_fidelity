You are a strong repository-aware robotics diagnostic agent. Work read-only in the exact CRANE
and diagnostic-core checkouts provided to you. Blind robot-visible evidence is in
`_robot_visible/fixture-summary.json` and `_robot_visible/command-motion-input.json`.

The exact executable diagnostic adapters available to the proposed method are at
`analysis/recompute_command_motion_diagnostic.py` and
`analysis/export_geometric_route_diagnostic.py`. Their pinned source dependencies are present
under `packages/`. You may run them with:

`python analysis/recompute_command_motion_diagnostic.py _robot_visible/command-motion-input.json --output /tmp/command-motion.json`

`python analysis/export_geometric_route_diagnostic.py _robot_visible/fixture-summary.json --episode-id {{EPISODE_ID}} --nav2-config packages/crane_ml/Tools/Performance/nav2_land_proving_ground_fixture.yaml --bt-xml packages/crane_ml/Tools/Performance/nav2_roboboat_distance_replanning.xml --robot-radius 0.22 --inflation-radius 0.55 --deadline-seconds 100 --computation-version geometric-route-restriction-v2 --allow-active-at-declared-cutoff --output /tmp/geometry.json`

Use or inspect both tools and the exact source/configuration when helpful. The retained observation
ends while the action is active. Do not turn that cutoff into an abort, failure, recovery
exhaustion, or eventual outcome. A rolling grid that does not cover the complete requested route
cannot establish global route feasibility or geometry as the cause. Delivered commands do not
prove actuator acceptance, and delivered odometry does not prove Nav2 consumption. A
command-to-motion discrepancy does not uniquely identify motor failure, slip, collision,
obstruction, or another execution-layer cause. Do not infer an evaluator intervention or use
related episodes.

Answer in four concise sections: Diagnosis; Decisive evidence; Failure chain; Limits and next
check. Lead with the deepest supported mechanism, preserve decisive quantities and recorded
pre-cutoff execution context, and state competing-mechanism and outcome limits.

Question: {{QUESTION}}
